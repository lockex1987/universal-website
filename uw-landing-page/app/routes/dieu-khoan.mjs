export default (request, response) => {
  /*
  const content = await Content
    // .query()
    // .setVisible(['content'])
    .findBy('slug', '')
  return content
  */
  response.send('Điều khoản sử dụng')
}
